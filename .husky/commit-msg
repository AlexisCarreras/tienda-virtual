echo ""
echo "🔍 Validando mensaje del commit..."
echo ""

if ! npx --no -- commitlint --edit "$1" 2>&1; then
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "❌  COMMIT RECHAZADO"
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo ""
  echo "El mensaje no cumple con Conventional Commits."
  echo ""
  echo "📝 FORMATO ESPERADO:"
  echo "   tipo(scope): descripción en minúsculas"
  echo ""
  echo "✅ EJEMPLOS VÁLIDOS:"
  echo "   feat(products): agrega filtro por talle"
  echo "   fix(cart): corrige cálculo del subtotal"
  echo "   chore: actualiza dependencias"
  echo "   refactor(auth): extrae hook useCurrentUser"
  echo "   docs: actualiza el readme"
  echo ""
  echo "📚 TIPOS PERMITIDOS:"
  echo "   feat     → nueva funcionalidad"
  echo "   fix      → corrección de bug"
  echo "   docs     → documentación"
  echo "   style    → formato (sin cambios de lógica)"
  echo "   refactor → reescritura sin cambiar comportamiento"
  echo "   test     → tests nuevos o modificados"
  echo "   chore    → mantenimiento, dependencias, config"
  echo "   perf     → mejora de performance"
  echo "   ci       → cambios en CI/CD"
  echo "   build    → cambios en sistema de build"
  echo "   revert   → revierte un commit anterior"
  echo ""
  echo "⚠️  REGLAS:"
  echo "   • La descripción debe estar en minúsculas"
  echo "   • No debe terminar con punto"
  echo "   • Máximo 100 caracteres en la primera línea"
  echo ""
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  exit 1
fi

echo ""
echo "✅ Mensaje del commit válido"
echo ""